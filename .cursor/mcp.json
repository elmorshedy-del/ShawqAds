{
  "mcpServers": {
    "agentmemory": {
      "command": "npx",
      "args": ["-y", "@agentmemory/mcp"],
      "env": {
        "AGENTMEMORY_URL": "${env:AGENTMEMORY_URL}",
        "AGENTMEMORY_SECRET": "${env:AGENTMEMORY_SECRET}"
      }
    }
  }
}
